[build]
builder = "nixpacks"
buildCommand = "pipx install poetry && poetry install"

[deploy]
startCommand = "poetry run uvicorn api.main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[nixpacks]
startCommand = "${NIXPACKS_START_CMD}"
